/**
 * Global CSS tokens for atp-stepper fallback if not defined in root theme
 */
export const atpStepperStyleGlobal = `
/* Add global level styling specific to stepper if needed, 
   like root transition variables or context classes */
`;
