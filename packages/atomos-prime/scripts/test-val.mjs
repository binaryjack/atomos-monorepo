import { f } from './node_modules/@binaryjack/formular.dev/dist/formular-dev.es.js'; import { createFormular } from './node_modules/@binaryjack/formular.dev/dist/formular-dev.es.js'; const schema = f.object({ label: f.string().optional() }); const form = createFormular({ schema }); try { const isValid = await form.validateForm(); console.log('isValid:', isValid); } catch (e) { console.error('THREW ERROR:', e); }
